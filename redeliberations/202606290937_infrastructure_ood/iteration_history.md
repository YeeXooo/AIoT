## 迭代第 1 轮

1. **问题描述**：`road_rage_voice_record` 表分片键 `vehicle_id` 不存在于表结构中
   - 所在位置：§3.1.5（分库分表策略）与 §3.1.1（表结构）的矛盾
   - 严重程度：严重
   - 改进建议：为该表增加冗余 `vehicle_id` VARCHAR(64) NOT NULL 列并补充到 §3.1.1 表结构中；或改为按 `driver_id` 哈希分片

2. **问题描述**：分片策略使用取模运算 `hash(vehicle_id) % 4` 而非一致性哈希，与 §3.1.5 的动态扩容目标和需求 §五.2 要求矛盾
   - 所在位置：§3.5.2
   - 严重程度：严重
   - 改进建议：将路由算法改为 ShardingSphere-JDBC 一致性哈希分片算法，或明确声明固定 4 片不扩容并给出全量重分布迁移方案

3. **问题描述**：EmergencyRescueService 被标注为边缘部署，但其职责依赖云端网络连通性
   - 所在位置：§3.3.6
   - 严重程度：一般
   - 改进建议：将标注改为"EmergencyRescueService（云端）"

4. **问题描述**：SafetyAlertEvent 主表上建有 `(driver_id, occurred_at DESC, alert_type, risk_level)` 和 `(vehicle_id, occurred_at DESC)` 两条复合索引，但读查询已全部路由至 CQRS 投影表，这些索引冗余且拖累写性能
   - 所在位置：§3.1.2
   - 严重程度：一般
   - 改进建议：主表仅保留 `(trip_id)` FK 索引，将两条读侧索引限定于 `alert_projection` 投影表

5. **问题描述**：EdgeSessionContext 在基础设施层未定义实例化方式、生命周期管理机制和进程重启恢复策略
   - 所在位置：§3.4.1、§3.4.2
   - 严重程度：一般
   - 改进建议：补充 EdgeSessionContext 的 Spring 集成方式、创建/销毁触发机制、与 VehicleStateBuffer/PhysiologicalDataBuffer 的绑定关系及重启恢复策略

6. **问题描述**：边缘侧仅描述了 RoadRageVoiceRecordRepository 的 SQLite 实现，TripRepository、DriverRepository、VehicleRepository、SystemAccountRepository 的边缘实现策略均未描述
   - 所在位置：§3.2.2
   - 严重程度：一般
   - 改进建议：新增"边缘侧仓储实现"小节，说明各仓储在 SQLite 环境下的实现策略

7. **问题描述**：outbox 表缺少指数退避重试所需的 `last_attempt_at` 字段，OutboxRelayer 无法判断各事件的重试等待期是否已过
   - 所在位置：§3.3.2 与 §3.3.5 的矛盾
   - 严重程度：一般
   - 改进建议：增加 `last_attempt_at TIMESTAMP NULL` 字段或 `next_retry_at TIMESTAMP NOT NULL` 字段

8. **问题描述**：MQTT Topic `iot/family/...` 的用途与家属 APP 不连接 MQTT 的说明不一致
   - 所在位置：§3.5.1
   - 严重程度：轻微
   - 改进建议：明确这两个 Topic 的用途（内部路由还是可删除）

9. **问题描述**：需求 §七.5 要求的部署架构图缺失，§3.7.2 仅覆盖 MQTT 信道拓扑
   - 所在位置：需求 §七.5 vs 设计 §3.7
   - 严重程度：轻微
   - 改进建议：增加 Mermaid flowchart 或 ASCII 文本图覆盖所有组件的网络连接关系

10. **问题描述**：VO-16 DrivingBehaviorCounters 被错误归入策略 B（@ElementCollection），实际为策略 A（@Embedded）
    - 所在位置：§3.1.3
    - 严重程度：轻微
    - 改进建议：将 VO-16 移至策略 A 表格中

11. **问题描述**：聚合根表主键生成策略未说明（应用层 UUID、JPA @GeneratedValue 还是金仓 UUID 函数）
    - 所在位置：§3.1.1
    - 严重程度：轻微
    - 改进建议：统一说明聚合根表主键采用应用层生成 UUID，理由为分布式无冲突生成

12. **问题描述**：边缘侧 SQLite 环境下乐观锁实现方案未说明（无法使用 JPA @Version）
    - 所在位置：§3.1.4
    - 严重程度：轻微
    - 改进建议：补充边缘侧 JDBC 层手动构造带版本号条件的 UPDATE 语句实现乐观锁

## 迭代第 2 轮

1. **问题描述**：`fleet_dashboard_projection` 投影表缺少主键或唯一约束定义，导致数据同步策略中依赖的 `INSERT ... ON CONFLICT DO UPDATE` 操作无法执行
   - 所在位置：§3.2.3 投影表 P2 字段定义
   - 严重程度：严重
   - 改进建议：明确 P2 表的主键或唯一约束，建议以 `(fleet_id, risk_level, alert_type)` 作为复合唯一约束；在字段表中显式标注主键/唯一约束列
2. **问题描述**：`trajectory_projection` 投影表的分片策略未明确，未纳入 §3.1.5 分片表清单，PK 为 BIGSERIAL 在跨分片环境下会产生主键冲突
   - 所在位置：§3.2.3 投影表 P3 vs §3.1.5 分片范围
   - 严重程度：一般
   - 改进建议：明确分片归属（建议按 `vehicle_id` 分片），PK 改为应用层生成 UUID 或复合分区键，补充到 §3.1.5 分片清单
3. **问题描述**：心跳监控机制缺少数据支撑——Vehicle 表中无 `last_heartbeat_at` 字段，心跳监控定时任务无法判断超时
   - 所在位置：§3.1.1 Vehicle 表结构 vs §3.7.5 降级策略表
   - 严重程度：一般
   - 改进建议：在 Vehicle 表中增加 `last_heartbeat_at TIMESTAMP NULL` 列，说明心跳监控定时任务的执行逻辑和更新组件
4. **问题描述**：Outbox 投递器轮询 SQL 查询条件存在逻辑缺陷——退避间隔为 per-event 变量，无法在单条 SQL 中动态计算
   - 所在位置：§3.3.2 索引设计 + §3.3.5 退避策略 + §4 场景 2 步骤 3
   - 严重程度：一般
   - 改进建议：明确实现方式（方案A：SQL 固定上界 60s + Java 层计算退避；方案 B：增加 `next_retry_at` 预计算列），同步修正三处描述的差异
5. **问题描述**：`guardianship` 表权限变更策略存在内部矛盾——DELETE 与 REVOKED 状态标记两句话矛盾，且历史记录无法保留
   - 所在位置：§3.1.2 表结构 + §3.1.4 说明 + §6.3 并发策略
   - 严重程度：一般
   - 改进建议：统一表述，明确实际策略（推荐保留历史记录方案，将 PK 改为三列组合或增加自增主键），同步修正 §6.3 并发策略描述
6. **问题描述**：IoTDA 上行消息消费机制不够具体，消费方式使用了"或"字未选定具体方案，无法指导编码实现
   - 所在位置：§3.5.1「IoTDA 上行消息消费适配器」小节
   - 严重程度：一般
   - 改进建议：选定一种具体方案（推荐 IoTDA 数据转发至 DMS Kafka），说明消费方式和消息体格式
7. **问题描述**：`fleet_dashboard_projection` 缓存策略存在路径断裂——事件驱动的缓存失效仅删 Redis key，不更新投影表，导致"即时重算"实际回退到已延迟的投影表数据
   - 所在位置：§3.2.3 投影表 P2 缓存/物化视图策略
   - 严重程度：一般
   - 改进建议：明确"即时重算"含义（即时增量聚合并同步更新投影表，或接受 ≤5 分钟延迟并明确声明 SLO）
8. **问题描述**：`@ElementCollection` 优化后集合被标记"已截断并冻结"，但未说明冻结的技术实现方式，JPA 脏检查可能触发级联 DELETE+INSERT 覆盖溢出数据
   - 所在位置：§3.1.3 策略 B 优化触发条件
   - 严重程度：一般
   - 改进建议：说明冻结的技术实现方式（不可变包装 / 清空引用 / 防御性标记），明确只读语义和异常行为
9. **问题描述**：NotificationPort 适配器的端口方法契约未在设计中呈现，开发者无法从设计文档获知需实现的方法签名和返回值约定
   - 所在位置：§3.4.6「NotificationPort 适配器」
   - 严重程度：一般
   - 改进建议：在 §3.4.6 开头增加端口接口方法列表（告警推送、SOS 救援推送、绩效预警推送、家属常态快照推送方法契约概要）
10. **问题描述**：OTA 升级的车载终端侧（接收端）设计缺失——分片接收、CRC32 校验、组装、刷写、回滚等边缘侧关键行为未涉及
    - 所在位置：§3.4.5 vs §3.7.1 边缘侧组件表
    - 严重程度：一般
    - 改进建议：新增"OTA 升级客户端（边缘侧）"小节，说明分片接收校验、组装刷写、回滚策略和断网续传逻辑
11. **问题描述**：RescueReportPort 的 HTTP API 备选投递路径缺乏接口规范（endpoint URL、请求方法/格式、认证方式、可重试状态码）
    - 所在位置：§3.4.7「RescueReportPort 适配器」
    - 严重程度：一般
    - 改进建议：定义备用 HTTP API 接口规范概要，或标注为"外部依赖待确认"并给出占位约定
12. **问题描述**：DEW 密钥轮转的离线车辆场景未覆盖——车辆离线期间未收到新密钥，重新上线后可能仍使用已标记为 DECRYPT_ONLY 的旧密钥加密新数据
    - 所在位置：§3.5.5「DEW 密钥管理与轮转策略」
    - 严重程度：一般
    - 改进建议：补充离线车辆密钥轮转处理策略（排队补推方案或宽限期方案），明确轮转过程中的密钥版本判定逻辑

## 迭代第 3 轮

1. **问题描述**：outbox 广播表策略下的跨分片事务原子性假设未验证——§3.5.2 断言广播表写入与分片表写入在同一个本地事务中由数据库本地事务保证原子性，但该前提在 ShardingSphere-JDBC LOCAL 模式下的实际行为未经官方文档或实验验证，存在事务原子性断裂风险
   - 所在位置：§3.5.2（金仓数据库连接与分片配置）
   - 严重程度：一般
   - 改进建议：标注 ShardingSphere-JDBC 目标版本号并说明已验证广播表写入与分片表写入的本地事务原子性；或给出后备方案（outbox 固定于 shard-0 接受最终一致性 / 引入 XA 分布式事务）

2. **问题描述**：fleet_dashboard_projection 即时增量聚合的跨分片 JOIN 可行性存疑——§3.2.3 P2 路径 A 描述实时 JOIN 三个分布在不同物理数据库的表，ShardingSphere 是否支持跨分片 JOIN 且性能满足 ≤3s SLO 未经验证
   - 所在位置：§3.2.3 P2 vs §3.5.2
   - 严重程度：一般
   - 改进建议：将即时聚合数据源改为从 `alert_projection`（同在 shard-0）聚合，补充 `fleet_id` 冗余字段避免跨分片；或明确标注依赖 ShardingSphere SQL Federation 引擎并说明已验证

3. **问题描述**：CQRS 投影表写入路由机制缺失——§3.5.2 将 `alert_projection` 和 `fleet_dashboard_projection` 配置为"全局只读表固定于 shard-0"，但未说明投影同步器通过 ShardingSphere-JDBC 写入时如何确保数据精确路由至 shard-0
   - 所在位置：§3.5.2 vs §3.2.3
   - 严重程度：一般
   - 改进建议：明确投影表写入路由方式——使用 HintManager 强制路由至 shard-0 / 配置独立 datasource 直连 shard-0 / 或将投影表作为广播表写入

4. **问题描述**：@ElementCollection 冻结技术实现中 Hibernate detach()+merge() 行为风险未充分评估——将集合字段置 null 后执行 detach()+merge() 可能导致 flush 时触发面向集合表的 DELETE 语句，意外删除已写入的溢出行数据
   - 所在位置：§3.1.3 策略 B「集合冻结技术实现」
   - 严重程度：一般
   - 改进建议：改用 StatelessSession 绕过一级缓存和脏检查仅执行 UPDATE；或标注为技术风险项需在编码阶段验证 Hibernate 实际行为；或维护 `snapshotCollectionFrozen` 标志让仓储层跳过级联保存

5. **问题描述**：文档一级标题版本号标注为 `v2`，与文件名表明的 v3 版本矛盾
   - 所在位置：文档第 1 行标题
   - 严重程度：轻微
   - 改进建议：将标题修正为 `（v3）`

6. **问题描述**：VehicleStateBuffer/PhysiologicalDataBuffer 描述为基于 `ArrayDeque`（固定容量）实现，但 JDK 标准 `ArrayDeque` 为无界队列，未说明如何实现固定容量语义
   - 所在位置：§3.4.1、§3.4.2
   - 严重程度：轻微
   - 改进建议：使用 `CircularFifoQueue` 或在每次 add 前检查容量并手动淘汰最旧元素，补充窗口溢出行为描述

7. **问题描述**：EdgeSessionContext destroy() 在 VehicleIgnitionOffEvent 触发时立即执行清空缓冲，但碰撞失能判定可能在此之后才检测到，导致无法获取事故前时间窗口数据
   - 所在位置：§3.4.2a vs §3.7.4
   - 严重程度：轻微
   - 改进建议：将 destroy() 延迟至判定引擎确认无待处理安全判定后执行，或熄火后先等待安全观察窗口再执行销毁

## 迭代第 4 轮

1. **问题描述**：IoTDA 上行消息消费与 outbox 写入之间存在事务边界断裂——Kafka 偏移量提交与数据库 outbox 写入分属独立事务，outbox 写入失败 + 偏移量已提交时告警事件静默丢失
   - 所在位置：§3.5.1 IoTDA 上行消息消费适配器 + §3.3.1 云端事件总线实现
   - 严重程度：严重
   - 改进建议：采用手动提交偏移量模式（enable.auto.commit=false），仅 outbox 写入成功后才提交偏移量；或补充分布式事务协调方案
2. **问题描述**：语音存证授权解密依赖云端 DEW 下发临时密钥，事故后车载终端离线导致解密链断裂，与需求 §六.2 三类授权场景的可用性要求矛盾
   - 所在位置：§3.6.2 授权解密段落 + §3.5.5 DEW 密钥管理
   - 严重程度：严重
   - 改进建议：增加离线解密路径（云端保存加密会话密钥副本、设备密钥包裹存储于 OBS）；增加现场解密路径（物理安全令牌 + DEW 预下发离线授权码）
3. **问题描述**：OutboxRelayer 多实例部署下 SELECT 与 UPDATE 之间存在时间窗口，可能导致同一条记录被多个实例投递至 Kafka，设计声称的"保证被唯一实例获取"不成立
   - 所在位置：§6.3 并发策略表 + §4 场景 2 步骤 3
   - 严重程度：一般
   - 改进建议：将 SELECT 改为 SELECT ... FOR UPDATE SKIP LOCKED，或明确承认 at-least-once 语义包含投递层面重复
4. **问题描述**：fleet_dashboard_projection 事件驱动即时增量聚合存在读-计算-写竞争窗口，并发告警事件可能导致增量聚合结果被覆盖丢失
   - 所在位置：§3.2.3 P2 缓存/物化视图策略路径 A
   - 严重程度：一般
   - 改进建议：在路径 A 描述中显式声明并发竞争取舍，注明路径 B 在下一个 5 分钟周期修正
5. **问题描述**：脱敏校验门控的具体挂载点、组件归属、Schema 文件路径、校验失败后完整行为链未明确，可编码性不足
   - 所在位置：§3.6.1 脱敏校验门控段落
   - 严重程度：一般
   - 改进建议：补充门控组件归属、挂载方式（interceptor 或前置处理器）、Schema 路径、校验失败后完整行为链
6. **问题描述**：家属 APP 告警通知存在 WebSocket / SMN / Push Kit 三条并行推送通道，APP 前台时可能收到重复通知，缺乏去重设计
   - 所在位置：§3.4.6 NotificationPort + §3.5.3 SMN 模板 + §3.7.2 部署图
   - 严重程度：一般
   - 改进建议：前台时仅走 WebSocket、后台时走 Push Kit，APP 侧基于 event_id 本地去重
7. **问题描述**：OTA 固件签名验证所需的 RSA-2048 公钥来源、预置方式、防篡改保护未说明，缺少可信根设计
   - 所在位置：§3.4.5 车载终端侧 OTA 客户端
   - 严重程度：一般
   - 改进建议：公钥预烧录至 TEE/KeyStore 安全存储，或通过设备主密钥 HMAC 签名保护
8. **问题描述**：guardianship 表部分唯一索引依赖 PostgreSQL 部分唯一索引语法，金仓数据库兼容性未经验证
   - 所在位置：§3.1.2 guardianship 关联表 + §6.3 并发策略
   - 严重程度：一般
   - 改进建议：补充金仓版本号及验证说明，给出金仓不支持时的悲观锁替代方案
9. **问题描述**：trip_physiological_snapshot 表被三条独立数据路径使用，但完整列清单从未显式定义，三条路径的实现者可能做出不一致假设
   - 所在位置：§3.1.3 策略 B + §3.2.2a + §3.7.4
   - 严重程度：一般
   - 改进建议：以表格形式定义完整列结构，标注各数据来源对写入列的义务

## 迭代第 5 轮

1. **问题描述**：§3.2.1 与 §3.2.2 中同一 JPQL 查询存在矛盾实现（`JOIN a.guardianships g` vs `JOIN guardianship g ON a.id = g.accountId`），其中 §3.2.1 的写法无法直接编译通过
   - 所在位置：§3.2.1 第 408 行 vs §3.2.2 第 425 行
   - 严重程度：严重
   - 改进建议：将 §3.2.1 表中的 JPQL 修正为与 §3.2.2 一致的跨表 JOIN 写法
2. **问题描述**：文档标题标注为"（v5）"，但修订说明从 v2 延续至 v10，标题版本号与内容成熟度严重不一致
   - 所在位置：文档第 1 行（标题）+ 修订说明（第 1349-1474 行）
   - 严重程度：严重
   - 改进建议：将标题修正为与实际修订层级一致的版本号（v10），明确产出版本号与修订说明版本号的坐标体系一致
3. **问题描述**：修订说明部分存在两个"修订说明（v5）"标题（第 1385 行和第 1391 行），构成排版错误
   - 所在位置：第 1385-1401 行
   - 严重程度：严重
   - 改进建议：合并两个 v5 批次为单一"修订说明（v5）"小节
4. **问题描述**：需求 §一.5 要求产出"金仓数据库的 CREATE TABLE DDL 概要（概念级）"，当前仅以 Markdown 表格列出字段，缺少 DDL 语法级别的结构展示（CREATE TABLE 语法、表级约束 SQL 表达）
   - 所在位置：需求 §一.5 vs 设计 §3.1.1-§3.1.3
   - 严重程度：一般
   - 改进建议：在 §3.1 末尾新增概念级 DDL 概要小节，以 CREATE TABLE 语法展示 1-2 张代表性表，含完整约束和代表性索引的 CREATE INDEX 语句
5. **问题描述**：DriverHealthProfile 表设计未显式声明独立查询路径——`@OneToOne`+`@MapsId` 虽不阻止独立查询，但缺少声明会使实现者默认通过 Driver 聚合加载而遗漏独立查询需求
   - 所在位置：需求 §一.3 vs 设计 §3.1.2
   - 严重程度：一般
   - 改进建议：在 §3.1.2 中增加独立查询路径声明，明确哪些健康指标字段可被独立检索及查询实现方式
6. **问题描述**：修订说明 v6、v7 中各有一条审查意见以 **REJECTED** 标记，该标记来源于审查质询内部流程，不应以原始形式出现在最终设计文档中，降低可读性
   - 所在位置：第 1407 行、第 1414 行
   - 严重程度：一般
   - 改进建议：将 REJECTED 替换为中性表述（如"补充说明"或"已核实并确认"），修订说明应面向最终读者
7. **问题描述**：OTA 刷写期间安全监测全部停止（≤2 分钟中断窗口），但设计未对"中断期间发生安全事件如何处理"给出任何陈述，未标注安全风险或明确对应用层/领域层的约束要求
   - 所在位置：§3.4.5 第 765 行
   - 严重程度：一般
   - 改进建议：补充 OTA 刷写前置条件声明——触发刷写前校验车辆状态为驻车熄火，行驶中仅允许接收和暂存升级包，禁止进入刷写阶段
8. **问题描述**：VehicleStateBuffer.getSnapshots 方法签名（`getSnapshots(tripId, window)`）与需求端口契约（`getSnapshotWindow(Instant, Instant)`）不一致
   - 所在位置：需求 §四.1 vs 设计 §3.4.1
   - 严重程度：一般
   - 改进建议：统一端口方法签名，若变更有理由则记录于修订记录
9. **问题描述**：PhysiologicalDataBuffer.getReadings 方法签名（`getReadings(tripId, window)`）与需求端口契约（`getPhysiologicalWindow(Instant, Instant)`）不一致
   - 所在位置：需求 §四.2 vs 设计 §3.4.2
   - 严重程度：一般
   - 改进建议：统一端口方法签名，与 3.2 一并说明变更理由
10. **问题描述**：guardianship 广播表上 `SELECT ... FOR UPDATE` 悲观锁替代方案未说明在 ShardingSphere-JDBC 中跨分片的实际执行行为——是仅锁定单个分片还是广播至所有分片
    - 所在位置：§3.1.2 第 266 行 vs §3.5.2 广播表配置
    - 严重程度：一般
    - 改进建议：明确广播表上 SELECT...FOR UPDATE 的实际行为，或改用非数据库层分布式锁（如 Redis SET NX）
11. **问题描述**：OTADeliveryPort 端口方法契约不完整——deliverPackage 的完整方法签名、参数类型、返回值约定、异常声明均未以接口形式呈现，进度回调接口未定义
    - 所在位置：§3.4.5
    - 严重程度：一般
    - 改进建议：参考 NotificationPort 的"端口方法契约概要"格式，为 OTADeliveryPort 定义完整的方法契约列表
12. **问题描述**：MediaSessionPort 的会话超时与并发限制未定义——最大持续时长、空闲超时、并发会话数限制均遗漏
    - 所在位置：§3.4.8
    - 严重程度：一般
    - 改进建议：补充会话生命周期的边界约束（最大持续时长、空闲超时、并发限制）
13. **问题描述**：outbox/DLQ 表在 DMS Kafka 长时间不可用时的存储膨胀风险未讨论——无 outbox 积压降级策略，无 DLQ 自动清理策略
    - 所在位置：§3.3.2 outbox 表结构 + §3.3.5 重试策略
    - 严重程度：一般
    - 改进建议：补充 outbox 积压监控告警阈值和降级策略，为 DLQ 表增加基于 moved_at 的时间保留窗口清理策略
14. **问题描述**：边缘侧 SQLite 核心持久化表（trip、safety_alert_event、road_rage_voice_record）在数据同步至云端后无清理策略，长期运行可能导致存储空间不足
    - 所在位置：§3.7.4 vs SQLite 核心表
    - 严重程度：一般
    - 改进建议：补充边缘侧 SQLite 核心表清理策略——已同步记录按 created_at 保留窗口清理（如保留最近 30 天），RoadRageVoiceRecord 按 expiry_time 清理
15. **问题描述**：guardianship 表描述中存在格式化 artifact `\n\n`（第 266 行），影响 Markdown 渲染
    - 所在位置：第 266 行
    - 严重程度：轻微
    - 改进建议：改为标准 Markdown 空行分隔
16. **问题描述**：domain_event_dlq 表主键 dlq_id 使用 BIGSERIAL，与文档声明的一主键策略（应用层生成 UUID）不一致
    - 所在位置：第 7 行 vs §3.3.3 第 601 行
    - 严重程度：轻微
    - 改进建议：将 dlq_id 改为 VARCHAR(64) UUID，或明确标注不适用主键策略的理由
17. **问题描述**：看板投影 P2 中 driver_count 聚合语义使用 alert_projection 为数据源，但口径概要声明使用 safety_alert_event，数据源命名不统一
    - 所在位置：§3.2.3 P2 路径 A（第 506 行）vs 聚合计算口径概要（第 511 行）
    - 严重程度：轻微
    - 改进建议：统一数据源命名为 alert_projection，或注明二者数据等价
18. **问题描述**：CameraOcclusionDetectionPort 的 OcclusionDetectedSignal 和 OcclusionRemovedSignal 载荷结构未定义
    - 所在位置：§3.4.4
    - 严重程度：轻微
    - 改进建议：补充信号载荷字段概要（至少含时间戳），或标注模拟阶段暂不定义
19. **问题描述**：NotificationPort 各方法接收的 Command 对象（PushAlertCommand、PushRescueReportCommand、PushPerformanceWarningCommand）载荷字段未定义
    - 所在位置：§3.4.6
    - 严重程度：轻微
    - 改进建议：为三种 Command 对象补充字段概要表
20. **问题描述**：IoTDA 离线消息缓存最长 24 小时——车辆离线超过 24 小时后重新上线时的数据弥补策略未定义，边缘侧本地缓冲与 IoTDA 缓存上限之间的协调未说明
    - 所在位置：§3.7.5 + §3.7.4
    - 严重程度：轻微
    - 改进建议：明确边缘侧断网超 24 小时场景同步策略——断网恢复后扫描 SQLite 中未确认记录批量补推
21. **问题描述**：DEW 密钥服务不可用时边缘侧缓存密钥的有效期未定义，若缓存密钥同时丢失（如进程重启）时的降级路径未定义
    - 所在位置：§3.5.5 灾备段
    - 严重程度：轻微
    - 改进建议：补充 DEW 不可用时的分级降级策略（短期使用缓存密钥，长期触发审计告警并用临时随机密钥加密）

## 迭代第 6 轮

1. **问题描述**：`domain_event_outbox.event_id` 字段类型为 PostgreSQL 原生 `UUID`，与统一主键策略（所有主键均为 `VARCHAR(64)`）不一致，`domain_event_dlq.dlq_id` 已修正但 outbox 表未同步
   - 所在位置：§3.3.2（第 641 行）
   - 严重程度：一般
   - 改进建议：将 `domain_event_outbox.event_id` 类型改为 `VARCHAR(64)`，与统一主键策略保持一致；或在统一主键策略中补充例外说明

2. **问题描述**：§3.1.6 概念级 DDL 中外键约束（Trip→Driver、Trip→Vehicle 等）在分片环境下是否可在数据库层实际创建并生效未经讨论，ShardingSphere-JDBC 管理多物理数据库时跨分片物理外键可行性存疑
   - 所在位置：§3.1.6 DDL（第 411-431 行、第 437-454 行）vs §3.1.5 分片策略（第 399-403 行）
   - 严重程度：一般
   - 改进建议：补充说明 FK 约束在分片环境下为逻辑约束（文档级声明），实际不创建物理外键；或确认 ShardingSphere-JDBC 5.x 支持并向注明验证状态

3. **问题描述**：guardianship 并发控制方案以 Redis `SET NX` 分布式锁为唯一机制，Redis 不可用时所有 guardianship 权限操作将全部失败，缺少降级路径，紧急救援场景下可能产生安全后果
   - 所在位置：§3.1.2（第 274 行）
   - 严重程度：一般
   - 改进建议：补充 Redis 不可用时的降级策略——短期故障重试、长期不可用降级为无锁模式依赖数据库级约束兜底，并通过 SMN 告警通知运维

4. **问题描述**：数据脱敏校验仅在 MQTT 上行路径执行，未覆盖边缘侧 SQLite 本地持久化路径，断网缓冲场景下原始图像可能通过本地持久化意外留存，违反需求"不写入任何持久化存储"约束
   - 所在位置：§3.6.1（第 1042-1048 行）vs §3.7.4
   - 严重程度：一般
   - 改进建议：将脱敏校验门控扩展至数据写入 SQLite 之前的持久化路径，或声明本地持久化写入同样经过 `SecurityGateFilter` 校验

5. **问题描述**：Vehicle 表 `fleet_id` 字段的写入时机和管理组件未定义，下游投影表依赖该值但来源不明确
   - 所在位置：§3.1.1 Vehicle 表（第 143 行）vs §3.7.1 组件部署表
   - 严重程度：轻微
   - 改进建议：补充 `fleet_id` 的来源（车辆注册时从 IoTDA 设备元数据获取写入）、可变性和对下游投影表的级联影响声明

6. **问题描述**：guardianship"撤销后再授予"场景下新旧行时序语义未定义，`granted_at` 生成约定和优先级判定规则缺失
   - 所在位置：§3.1.2（第 266-268 行）vs §3.1.4（第 381 行）
   - 严重程度：轻微
   - 改进建议：补充 `granted_at` 由应用层 `Instant.now()` 显式赋值保证单调递增，声明当前有效授权判定规则为 `revoked_at IS NULL ORDER BY granted_at DESC LIMIT 1`

7. **问题描述**：`MediaSessionPort` 方法命名与需求不一致（`terminateSession` vs `endSession`）、`SessionType` 枚举值扩展超出需求范围但未记录变更理由
   - 所在位置：§3.4.8（第 910 行）vs 需求 §四.8
   - 严重程度：轻微
   - 改进建议：统一端口方法命名与需求一致，在方法列表中标注对应关系；显式列出 `SessionType` 枚举值
